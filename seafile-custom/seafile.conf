[fileserver]
port=8082
# web_token_expire_time 控制同步客户端 token 的 TTL，不控制下载 access token。
# Seafile 13 下载 access token 是一次性消费的（seaf-server 硬编码）。
# 下载重复请求问题已通过 nginx proxy_cache 解决（见 seafile.nginx.conf 的 /seafhttp/files/ location）。
web_token_expire_time = 600
